/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['res.cloudinary.com'], // Добавляем разрешение для Cloudinary
    },
  };
  
  export default nextConfig;
  